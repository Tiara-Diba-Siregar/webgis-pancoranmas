import React from "react";

const NotFound = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "100vh",
        background: "#f0f0f0", // Set your desired background color
      }}
    >
      <h1
        style={{
          fontSize: "3em", // Set your desired font size
          color: "#333", // Set your desired text color
        }}
      >
        NOT FOUND
      </h1>
    </div>
  );
};

export default NotFound;
